
services:
    main:
        build: 
            context: .
            dockerfile: ./docker/main/Dockerfile
        volumes:
            - .:/app

    pytest:
        build: 
            context: .
            dockerfile: ./docker/pytest/Dockerfile
        volumes:
            - .:/app
